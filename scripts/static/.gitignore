node_modules
# Note: dist/ is COMMITTED for test_compile so the Python server can serve
# the built UI without requiring a node toolchain on the user's machine.
# Do NOT add dist/ here.
.vite
*.tsbuildinfo
