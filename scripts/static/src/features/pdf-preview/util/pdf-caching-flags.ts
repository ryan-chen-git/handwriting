// Stub of Overleaf's pdf-caching-flags. We ship no caching layer, so every
// flag is disabled.
export const trackPdfDownloadEnabled = false;
export const enablePdfCaching = false;
export const prefetchingEnabled = false;
export const prefetchLargeEnabled = false;
export const cachedUrlLookupEnabled = false;
