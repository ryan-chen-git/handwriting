// Overleaf's shim for older browsers missing AbortSignal.throwIfAborted /
// AbortSignal.any. Modern evergreen browsers ship both natively, so this file
// is an intentional no-op on our side.
export {};
